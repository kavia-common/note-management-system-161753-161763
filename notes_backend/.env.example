# Environment variables required by the Notes Backend.
# Copy this file to .env and set appropriate values for your environment.

# SQLite (default) or any ADO.NET connection string supported by EF Core
DB_CONNECTION_STRING=Data Source=notes.db

# JWT settings (MUST be set in production)
JWT__KEY=your-very-strong-secret-key
JWT__ISSUER=notes-app
JWT__AUDIENCE=notes-web
